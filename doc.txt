相关的组建

核心模块，包括用户系统，文件系统，集群管理，搜索，配额，存储对象等子系统并提供API库

TOKEN/数据库连接/HTTP连接等用户资源缓存管理,提供数据库和文件系统同步工具，支持MD5

存储方式，并提供容灾恢复方案。底层md5存储采用一致性hash存储实现方式。

采用分层设计，api层，mission层，events调度层，后端封装数据库和存储接口

使用HTTP协议进行各组件间各层通信，URL自定义路由

数据库采用ORM设计

集群监控管理，使用生产者和消费者模型，节点硬件静态数据，内存，CPU，网络，IO读写，存储运行数据

设计Mirror层，缓存动态数据，减少数据库交互，递增sequence序列号和分类存储,采用多线程多消息队列实现

系统服务监控，通过在process监控的基础上实现service监控,process汇总分析，

包括代理服务，对象服务，账户服务，容器服务等进程即其服务运行状态统计

配置管理 实现分布式paxos系统。

组件设计，MIRROR,ORM,一致性哈希，PAXOS

